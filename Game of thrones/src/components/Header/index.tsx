import React from 'react';

import { HeaderContainer } from './styles';

const Header: React.FC = () => {
  return (
    <HeaderContainer>
      <a href="/">Home</a>
    </HeaderContainer>
  );
};

export default Header;
